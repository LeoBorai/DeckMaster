{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "lint": "eslint . --ext .ts,.tsx --fix"
  }
}
